#include "spkmeans.h"
#include <stdio.h>
#include <string.h>

int main (int argc, char **argv) {
    int k;
    char *input_filename;

    if 
    k = atoi(argv[1]);
    input_filename = argv[argc-2];
}